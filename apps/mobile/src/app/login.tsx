// Socratic-Mobile/app/login.tsx
// This file defines the '/login' route for Expo Router.
// It simply imports and exports the actual screen component from the /src directory.

import LoginScreen from '@features/auth/LoginScreen'; // Adjust path if needed (e.g., ../src/screens/LoginScreen)

export default LoginScreen; // Re-export the component